# Copy this file to .env and fill values before running locally
# MongoDB connection string (use MongoDB Atlas connection string)
MONGODB_URI=
# JWT secret key
JWT_SECRET=super_secret_here
# Comma-separated allowed origins for CORS (leave empty to allow any)
ALLOWED_ORIGINS=https://abdalla-dev.vercel.app
# Port to run the server on
PORT=5000
